=== WIADOMOSC OD PRZEMKA (2026-09-06, rano) ===

TWOJA NAPRAWA STDIN PRZESZLA NA WINDOWS. Dowod zebrany, nie opinia.

1. ZYWY CLI NA WINDOWS — 137 s, $0,4103, kod wyjscia 0

   `Generator.Cli` -> RunRoundAsync, wersja pierwsza z opisu (ta sama sciezka co Twoje
   $0,127 na macOS). Brief dobralem tak, zeby byl testem Twojej poprawki: polskie znaki,
   `%`, `&` i cudzyslowy w jednym zdaniu.

   W snapshocie WSZYSTKO wrocilo nietkniete:
     - „Zolty Zonkil", „Swietochlowice", „Lakowa 7", „slubne", „wiazanki" — z ogonkami,
       po 5-6 wystapien kazde
     - „100 zl" i „10%" — czyli metaznak przeszedl
     - plik w UTF-8, `<meta charset="utf-8">`, kotwice `data-cmt-id` w kebab-case
       (naglowek-glowny, hero-powitanie, pasek-korzysci, oferta-bukiety-slubne...)

   Przed Twoja poprawka ta sama droga nie oddawala JSON-a w ogole. To jest ten dowod na
   cmd.exe, ktorego z macOS nie dalo sie zebrac.

2. KOSZT 3,2x WYZSZY NIZ U CIEBIE — I NIE JEST TO ROZNICA SYSTEMU

   $0,4103 vs Twoje $0,1274. Roznica siedzi w BRIEFIE, nie w Windows: moj mial dziesiec
   faktow (godziny, ceny, rabat, dowoz, telefon), a model oddal 8,5 kB HTML + 8,8 kB CSS.
   Wniosek do §4.2, ktory wpisalem: o koszcie wersji pierwszej rozstrzyga opis klienta,
   a nie system — i Twoje $0,50-1,00 z ekstrapolacji nie bylo zawyzone.

3. CZAS: 137 s — ALE TO NIE JEST CZAS PROPOZYCJI

   `Generator.Cli` nie dotyka `RunProposalsAsync`, wiec czasu TRZECH PROPOZYCJI nadal
   nie mamy. Zeszedlem za to z obietnicy na ekranie „Buduje Twoja strone" z „okolo
   4 minut" na „okolo 2-3 minut" — 137 s to zmierzone wywolanie tego samego ksztaltu.
   Ekran trzech propozycji zostaje przy 4-5 minutach, bo zanizanie niezmierzonej
   sciezki byloby zgadywaniem w gorsza strone.

4. FLAKY — POMIAR ZROBIONY, I TWOJA POPRAWKA NIE SIEGA MIEJSCA, W KTORYM TO WYCHODZI

   Sam silnik: 10/10 czysto. Pelny `dotnet test` (oba zestawy rownolegle): 1 czerwony
   na 25 przebiegow, potem 15 z rzedu czysto.

   Ale za kazdym razem INNY test i zaden z nich to nie `Nieudana_runda_przywraca_workdir`:
   raz `VersionStoreTests.Restore_przywraca_starsza_wersje...`, raz
   `Rollback_przestawia_biezaca_wersje_nie_kasujac_historii`.

   Oba siedza w operacjach na KATALOGACH (kopiowanie/kasowanie snapshotu), a `ZapisAtomowy`
   jest uzywany tylko w `ProjectStore` i `CommentStore` — czyli przy zapisie PLIKU.
   Twoja hipoteza (b) o przejsciowej blokadzie wyglada wiec na trafna, ale ponawianie
   nie obejmuje `DirectoryOps.Copy` ani `Restore`. Jesli chcesz to domknac, to tam.

   Nie zdazylem zlapac tresci wyjatku (15 kolejnych przebiegow bylo czystych) — jak
   znowu wpadnie, dosle.

5. U MNIE OD WCZORAJ

   - `/panel` — panel wewnetrzny dla nas dwojga: lista projektow, koszty, podglady.
     Bez `Admin:Token` trasy NIE MA (404). Nie drukuje tokenow klientow.
   - EKRAN ODBIORU — klient wreszcie moze zabrac swoja strone. Twoj `/api/.../zip` jest
     `Chroniony`, a przegladarka nie wysle `X-Project-Token` ze zwyklego `<a href>`,
     wiec z UI byl nieosiagalny i decyzja 2 konczyla sie na papierze. Doszla trasa
     `/pobierz/{id}/{n}` (bez tokenu, jak `/preview`) i wspolna `Paczka`.
     Twoj fix z separatorem `/` w nazwach wpisow ma teraz test, ktory bez niego pada.
   - Wszystko po Twoich dziewieciu commitach: 193/193 web.

--- Ta wiadomosc wisi, dopoki jej nie skasujesz: rm .claude/wiadomosc.txt ---
